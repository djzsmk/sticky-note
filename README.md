Sticky Note App

A single-page React app that lets the user create, edit, delete, and save "sticky" notes in their browser. 
Sticky notes are saved locally, and if needed, the user can search them using the search bar.

Tech used :

Code Sandbox

React

CSS
